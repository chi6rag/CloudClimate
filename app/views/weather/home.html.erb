<div class="weather_home">
	<%= form_tag(results_path, method: :post, class: 'search_city_form') do  %>
		<%= text_field_tag :location, nil, { class: 'search_city_field' } %>
		<input name="lat" type="hidden" value="">
    <input name="lng" type="hidden" value="">
		<%= submit_tag "Find", data: { disable_with: "Searching..." } %>
	<% end %>
</div>